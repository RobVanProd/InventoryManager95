{
  "name": "inventory-management-backend",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd inventory-management-ui && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
